<nav class="navbar is-dark" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item has-text-white has-text-weight-bold" href="/">TowerDNS</a>
        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="mainNavbar">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>
    <div id="mainNavbar" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item{% if active == 'zones' %} has-background-dark{% endif %}" href="/zones">
                Zonen
            </a>
            {% if user.hasPermission(constant('TowerDNS\\Domain\\Auth\\Permission::USER_MANAGE')) %}
            <a class="navbar-item{% if active == 'users' %} has-background-dark{% endif %}" href="/users">
                Benutzer
            </a>
            {% endif %}
            {% if user.hasPermission(constant('TowerDNS\\Domain\\Auth\\Permission::ROLE_MANAGE')) %}
            <a class="navbar-item{% if active == 'roles' %} has-background-dark{% endif %}" href="/roles">
                Rollen
            </a>
            {% endif %}
            {% if user.hasPermission(constant('TowerDNS\\Domain\\Auth\\Permission::SYSTEM_SETTINGS_MANAGE')) %}
            <a class="navbar-item{% if active == 'settings' %} has-background-dark{% endif %}" href="/settings">
                Einstellungen
            </a>
            {% endif %}
            {% if user.hasPermission(constant('TowerDNS\\Domain\\Auth\\Permission::PROVIDER_CREDENTIALS_MANAGE')) %}
            <a class="navbar-item{% if active == 'credentials' %} has-background-dark{% endif %}" href="/credentials">
                Provider
            </a>
            {% endif %}
        </div>
        <div class="navbar-end">
            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link{% if active == 'profile' %} has-background-dark{% endif %}">
                    {{ user.displayName ?? user.email ?? '' }}
                </a>
                <div class="navbar-dropdown is-right is-boxed">
                    <a class="navbar-item" href="/profile">Mein Profil</a>
                    <hr class="navbar-divider">
                    <a class="navbar-item" href="/profile/password">Passwort ändern</a>
                    <a class="navbar-item" href="/profile/totp">Zwei-Faktor (TOTP)</a>
                    <a class="navbar-item" href="/profile/webauthn">Sicherheitsschlüssel</a>
                </div>
            </div>
            <div class="navbar-item">
                <form method="post" action="/logout" style="display:inline">
                    <input type="hidden" name="csrf_token" value="{{ csrfToken }}">
                    <button class="button is-small is-light" type="submit">Abmelden</button>
                </form>
            </div>
        </div>
    </div>
</nav>
